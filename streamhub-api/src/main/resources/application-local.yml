spring:
  datasource:
    url: jdbc:mysql://localhost:3306/streamhub?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
    username: streamhub
    password: streamhub
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

# S3-compatible object storage (MinIO) — used from Phase 2 onward.
storage:
  endpoint: http://localhost:9000
  region: us-east-1
  access-key: streamhub
  secret-key: streamhub123
  bucket: streamhub-media

# SQS via LocalStack.
spring.cloud.aws:
  region.static: ap-northeast-2
  credentials:
    access-key: test
    secret-key: test
  sqs.endpoint: http://localhost:4566
